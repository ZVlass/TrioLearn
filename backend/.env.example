# --- API keys ( empty in GitHub) ---
GOOGLE_BOOKS_API_KEY=
YOUTUBE_API_KEY=

# --- Data files (paths are relative to repo root; engine has sane defaults) ---
BOOKS_CSV=backend/data/interim/books_metadata.csv
COURSES_CSV=backend/data/courses/courses_metadata.csv
VIDEOS_CSV=backend/data/interim/videos_metadata.csv

BOOK_EMBS_NPY=backend/data/embeddings/books_embeddings.npy
COURSE_EMBS_NPY=backend/data/embeddings/courses_embeddings.npy
VIDEO_EMBS_NPY=backend/data/embeddings/videos_embeddings.npy

# --- Optional topic vectors ---
TOPIC_VECTORS_FILE=backend/data/topics/all_topic_vectors.parquet

# --- Models (used by ml_selector) ---
XGB_MODEL_PATH=backend/outputs/models/xgb_best_model.pkl
XGB_LABEL_ENCODER_PATH=backend/outputs/models/label_encoder.pkl

# --- Embedding model name (optional override) ---
EMBEDDING_MODEL=all-MiniLM-L6-v2
